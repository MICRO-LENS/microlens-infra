apiVersion: apps/v1
kind: Deployment
metadata:
  name: stain-detection
  namespace: microlens
  labels:
    app: stain-detection
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stain-detection
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: stain-detection
    spec:
      serviceAccountName: microlens-sa
      # init container가 S3에서 모델을 model-volume에 다운로드한 뒤
      # 메인 컨테이너가 /app/models 로 마운트해서 사용한다.
      initContainers:
        - name: download-model
          image: amazon/aws-cli:latest
          command:
            - aws
            - s3
            - cp
            - s3://microlens-model-weights/models/stain_detection_best.onnx
            - /models/stain_detection_best.onnx
          env:
            - name: AWS_DEFAULT_REGION
              value: "ap-northeast-2"
          volumeMounts:
            - name: model-volume
              mountPath: /models
      containers:
        - name: stain-detection
          # overlay의 images: 블록이 이 이름을 기준으로 ECR URL + 커밋 SHA 태그로 교체한다
          image: stain-detection-api:latest
          # SHA 태그는 불변이므로 IfNotPresent로 노드 캐시 활용 (Always 불필요)
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_BUCKET
              value: "microlens-model-weights"
            - name: AWS_REGION
              value: "ap-northeast-2"
            - name: MODEL_KEY
              value: "models/stain_detection_best.onnx"
          volumeMounts:
            - name: model-volume
              mountPath: /app/models
          resources:
            requests:
              cpu: "500m"
              memory: "2Gi"
              nvidia.com/gpu: "1"
            limits:
              cpu: "2"
              memory: "8Gi"
              nvidia.com/gpu: "1"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
      volumes:
        - name: model-volume
          emptyDir: {}
      nodeSelector:
        node-type: gpu
      tolerations:
        - key: dedicated
          operator: Equal
          value: gpu
          effect: NoSchedule
---
apiVersion: v1
kind: Service
metadata:
  name: stain-detection-svc
  namespace: microlens
spec:
  selector:
    app: stain-detection
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
